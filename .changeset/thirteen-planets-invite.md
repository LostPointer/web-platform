---
"@lostpointer/web-react": minor
---

Add `@lostpointer/web-react`: domain-neutral, plain HTML/CSS React primitives (`PageHeader`, `Card`/`CardHeader`, `SectionHeader`, `EmptyState`, `StatusBadge`, `Loading`, `ErrorState`, `FormActions`, `FormDialog`, `PageContainer`, `AppHeaderBar`) with Storybook coverage and keyboard/screen-reader tested behavior. React is the only peer dependency; components never import application DTOs, API clients, routing, or React Query.
